$Id$

To build the application make sure that the following packages are installed:

    libdb4.7-dev libdb4.7

To build the app:


    make

    You should end up with an executable file "./bin/kvs_bdb" 

To clean:

    make clean

EOF
